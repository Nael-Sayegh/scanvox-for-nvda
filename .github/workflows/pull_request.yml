name: pull_request

permissions:
  pull-requests: read


on:
  pull_request:
    types: [synchronize]


jobs:
  build:
    uses: Nael-Sayegh/github-action/.github/workflows/build.yml@create-action
    secrets: inherit

  generate_pot:
    uses: nael-sayegh/github-action/.github/workflows/generate_pot.yml@create-action
    secrets: inherit